version: 2

sources:
  - name: raw
    database: DATALENS_LAB
    schema: RAW
    tables:
      - name: customers
      - name: orders
      - name: stale_events
        loaded_at_field: loaded_at
        freshness:
          error_after:
            count: 24
            period: hour

